{
  "name": "clic-diversidade-functions",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Netlify Functions do CLIC Diversidade (agente Lelia + utilidades).",
  "scripts": {
    "dev:full": "netlify dev",
    "build": "node scripts/build.mjs",
    "preview": "npm run build && netlify dev --dir dist"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "cheerio": "^1.2.0"
  }
}
